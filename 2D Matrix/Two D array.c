#include<stdio.h>

int main()
{
    int r1,c1,r2,c2,sum,a[10][10],b[10][10],

    printf("enter the row number for matrix 1:");
    printf("\nenter the coloum number for matrix 1:");
    scanf("%d %d",&r1,&c1);
    printf("enter the row number for matrix 2:");
    printf("enter the coloum number for matrix 2:");
    scanf("%d %d",&r2,&c2);
    while(c1!=r2)
    {
        printf("error! the matrix is not mutiplicable");
        printf("enter row and col for 1st matrix:");
        scanf("%d %d",&r1,c1);
        printf("enter the row and col for matrix 2:");
        sccanf("%d %d",&r2,&c2);
    }
    printf("the first matrix is:");
    for(i=0,i<r1,i++)
    {
        for(j=0;j=c1;j++)
        {
            scanf("%d",&)
        }
    }





}
